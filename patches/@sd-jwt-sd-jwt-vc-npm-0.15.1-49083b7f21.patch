diff --git a/dist/index.js b/dist/index.js
index b79afc0..62f913c 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -326,6 +326,7 @@ var SDJwtVcInstance = class _SDJwtVcInstance extends import_core.SDJwtInstance {
   verifyStatus(result, options) {
     return __async(this, null, function* () {
       var _a, _b, _c, _d, _e;
+      if (options && options.skipStatusCheck) return;
       if (result.payload.status) {
         if (result.payload.status.status_list) {
           const fetcher = (_a = this.userConfig.statusListFetcher) != null ? _a : this.statusListFetcher.bind(this);
@@ -333,14 +334,22 @@ var SDJwtVcInstance = class _SDJwtVcInstance extends import_core.SDJwtInstance {
             result.payload.status.status_list.uri
           );
           const slJWT = import_core.Jwt.fromEncode(statusListJWT);
-          yield slJWT.verify(
-            (_b = this.userConfig.statusVerifier) != null ? _b : this.userConfig.verifier,
-            options
-          );
+          try {
+            yield slJWT.verify(
+              (_b = this.userConfig.statusVerifier) != null ? _b : this.userConfig.verifier,
+              options
+            );
+          } catch (e) {
+            throw new Error("Status list token verification failed: " + (e && e.message ? e.message : String(e)));
+          }
           const currentDate = (_c = options == null ? void 0 : options.currentDate) != null ? _c : Math.floor(Date.now() / 1e3);
+          /**
+           * Already testest above, and we do not want to throw
+           *
           if (((_d = slJWT.payload) == null ? void 0 : _d.exp) && slJWT.payload.exp < currentDate) {
             throw new import_utils.SDJWTException("Status list is expired");
           }
+           */
           const statusList = (0, import_jwt_status_list.getListFromStatusListJWT)(statusListJWT);
           const status = statusList.getStatus(
             result.payload.status.status_list.idx
diff --git a/dist/index.mjs b/dist/index.mjs
index 2d25177..af3e6f7 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -294,6 +294,7 @@ var SDJwtVcInstance = class _SDJwtVcInstance extends SDJwtInstance {
   verifyStatus(result, options) {
     return __async(this, null, function* () {
       var _a, _b, _c, _d, _e;
+      if (options && options.skipStatusCheck) return;
       if (result.payload.status) {
         if (result.payload.status.status_list) {
           const fetcher = (_a = this.userConfig.statusListFetcher) != null ? _a : this.statusListFetcher.bind(this);
@@ -301,14 +302,22 @@ var SDJwtVcInstance = class _SDJwtVcInstance extends SDJwtInstance {
             result.payload.status.status_list.uri
           );
           const slJWT = Jwt.fromEncode(statusListJWT);
-          yield slJWT.verify(
-            (_b = this.userConfig.statusVerifier) != null ? _b : this.userConfig.verifier,
-            options
-          );
+          try {
+            yield slJWT.verify(
+              (_b = this.userConfig.statusVerifier) != null ? _b : this.userConfig.verifier,
+              options
+            );
+          } catch (e) {
+            throw new Error("Status list token verification failed: " + (e && e.message ? e.message : String(e)));
+          }
           const currentDate = (_c = options == null ? void 0 : options.currentDate) != null ? _c : Math.floor(Date.now() / 1e3);
+          /**
+           * Already testest above, and we do not want to throw
+           *
           if (((_d = slJWT.payload) == null ? void 0 : _d.exp) && slJWT.payload.exp < currentDate) {
             throw new SDJWTException("Status list is expired");
           }
+          */
           const statusList = getListFromStatusListJWT(statusListJWT);
           const status = statusList.getStatus(
             result.payload.status.status_list.idx
diff --git a/src/sd-jwt-vc-instance.ts b/src/sd-jwt-vc-instance.ts
index 76c4085..8ed5d7d 100644
--- a/src/sd-jwt-vc-instance.ts
+++ b/src/sd-jwt-vc-instance.ts
@@ -341,6 +341,9 @@ export class SDJwtVcInstance extends SDJwtInstance<SdJwtVcPayload> {
     result: VerificationResult,
     options?: VerifierOptions,
   ): Promise<void> {
+    if ((options as { skipStatusCheck?: boolean })?.skipStatusCheck) {
+      return;
+    }
     if (result.payload.status) {
       //checks if a status field is present in the payload based on https://www.ietf.org/archive/id/draft-ietf-oauth-status-list-02.html
       if (result.payload.status.status_list) {
@@ -358,18 +361,26 @@ export class SDJwtVcInstance extends SDJwtInstance<SdJwtVcPayload> {
           StatusListJWTPayload
         >(statusListJWT);
         // check if the status list has a valid signature. The presence of the verifier is checked in the parent class.
-        await slJWT.verify(
-          this.userConfig.statusVerifier ??
-            (this.userConfig.verifier as Verifier),
-          options,
-        );
+        try {
+          await slJWT.verify(
+            this.userConfig.statusVerifier ??
+              (this.userConfig.verifier as Verifier),
+            options,
+          );
+        } catch (e) {
+          throw new Error('Status list token verification failed: ' + (e && (e as Error).message ? (e as Error).message : String(e)));
+        }
 
         const currentDate =
           options?.currentDate ?? Math.floor(Date.now() / 1000);
+        /**
+         * Already testest above, and we do not want to throw
+         *
         //check if the status list is expired
         if (slJWT.payload?.exp && (slJWT.payload.exp as number) < currentDate) {
           throw new SDJWTException('Status list is expired');
         }
+        */
 
         // get the status list from the status list JWT
         const statusList = getListFromStatusListJWT(statusListJWT);
