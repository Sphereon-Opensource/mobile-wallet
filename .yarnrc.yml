# .yarnrc.yml
patchFolder: "./patches"

nodeLinker: node-modules
winLinkType: symlinks

# Turn `@example/package` into `@example+package`, then tack on +version.patch
#patchFileNameTemplate: "${name.replace(/\\//g,'+')}+${source.reference}.patch"